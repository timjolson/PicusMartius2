#For VU7 (7" touchscreeen HDMI)
#backup boot file
sudo cp /media/boot/boot.ini /media/boot/backup.boot.ini
sudo nano /media/boot/boot.ini

# uncomment the resolution line below and comment out any other resolution lines

# 1024x600 without monitor data using generic information 
setenv videoconfig "drm_kms_helper.edid_firmware=edid/1024x600.bin"

# comment hdmi, uncomment dvi; should look like the following:

# - HDMI Mode 
# setenv vout "hdmi" 
# - DVI Mode (disables sound over HDMI as per DVI compat) 
setenv vout "dvi"